{
  "name": "cors-solver-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "A lightweight CORS solver proxy that forwards all requests to a target URL.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "http-proxy": "^1.18.1",
    "morgan": "^1.10.0"
  }
}
